{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify && npm run copy-html",
    "copy-html": "mkdir dist 2>/dev/null; cp *.html dist/ 2>/dev/null || copy *.html dist\\ 2>/dev/null || xcopy *.html dist\\ /Y 2>/dev/null"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "tailwindcss": "^3.4.18"
  }
}